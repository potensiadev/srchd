# JD 추출 고도화 기획안 v2

## 1. 배경 및 목적

### 1.1 현재 문제점
- JD 원문의 풍부한 정보가 **요약/태그화 과정에서 손실**됨
- 자격요건의 **맥락과 뉘앙스**가 사라짐 (예: "5년 이상" → 숫자만 추출)
- 직무별로 **다양한 형태의 요건**을 유연하게 처리하지 못함

### 1.2 목표
- JD 원문을 **최대한 보존**하여 정보 손실 최소화
- 다양한 JD 포맷에 **유연하게 대응**
- 시맨틱 매칭 품질 향상을 위한 **풍부한 컨텍스트 확보**

---

## 2. 신규 필드 정의

### 2.1 주요업무 (responsibilities)

**정의:** 해당 포지션에서 수행할 업무, 역할, 담당 범위

**추출 대상 섹션 (참고용, 하드코딩 X):**
- "주요업무", "담당업무", "수행업무", "업무내용"
- "Role & Responsibilities", "What you'll do"
- "■ 수행 업무", "[ 담당 업무 ]" 등

**추출 원칙:**
- 원문 그대로 추출 (bullet point, 번호 등 형식 유지)
- 요약하지 않음
- 해당 섹션이 명확히 구분되어 있을 때만 추출

**예시 (실제 추출 결과):**
```
- 고객사 AI 사업 사전 컨설팅
- AI 사업 기술 검토 및 PoC 지원
- 핵심 기술 성능지표 관리
- AI 기술 트렌드 센싱 및 분석
```

---

### 2.2 자격요건 (qualifications)

**정의:** 지원자가 반드시 갖춰야 하는 필수 조건, 경험, 역량

**특징:**
- **직무/산업별로 매우 다양한 형태**로 작성됨
- 경력, 학력, 스킬, 경험, 자격증, 언어 등 복합적 요건 포함
- 정량적 조건(년수)과 정성적 조건(경험 유형)이 혼재

**추출 대상 섹션 (참고용, 하드코딩 X):**
- "자격요건", "필수요건", "지원자격", "필수 사항"
- "Requirements", "Qualifications", "Must have"
- "■ 필요 경험/경력", "[ 자격 요건 ]" 등

**추출 원칙:**
- 원문 그대로 추출
- 조건들의 맥락과 뉘앙스 보존
- 요약/축약 절대 금지

**다양한 예시 (직무별 차이 참고):**

```
[블록체인/Web3 직무]
- Web3, 블록체인, 디지털 자산 관련 업무 경력을 5년 이상 보유하신 분
- 스마트 컨트랙트 개발 경험이 있으신 분
- DeFi, NFT 등 Web3 생태계에 대한 깊은 이해

[교육/강의 직무]
- 기술 교육, 강의 또는 멘토링 경험을 2년 이상 보유하신 분
- 복잡한 기술 개념을 쉽게 설명할 수 있는 커뮤니케이션 능력

[백엔드 개발 직무]
- RESTful API를 직접 설계, 개발하고 명확한 문서를 작성한 경험
- 대규모 트래픽 처리 경험
- MSA 아키텍처 설계 및 운영 경험

[글로벌/해외영업 직무]
- 비즈니스 영어 커뮤니케이션이 유창하신 분
- 해외 파트너사 협업 경험 3년 이상
- 글로벌 시장에 대한 이해와 문화적 감수성

[일반 경력직]
- 학사(4년제) 이상
- 관련 직무 경력 3년 이상
- 해당 분야 자격증 보유자
```

---

### 2.3 우대사항 (preferredQualifications)

**정의:** 필수는 아니지만 보유 시 우대하는 조건, 경험, 역량

**특징:**
- 자격요건과 유사하나 "있으면 좋은" 조건
- Plus factor로 작용
- 직무별로 다양한 형태

**추출 대상 섹션 (참고용, 하드코딩 X):**
- "우대사항", "우대요건", "우대 조건"
- "Preferred", "Nice to have", "Plus"
- "■ 우대 사항", "[ 이런 분이면 더 좋아요 ]" 등

**추출 원칙:**
- 원문 그대로 추출
- 요약/축약 금지

**다양한 예시:**

```
[홍보/PR 직무]
- 글로벌 기업 홍보/커뮤니케이션 전략 수립 및 실행 경력
- 해외법인 조직 및 커뮤니케이션 유경험자
- 위기관리 커뮤니케이션 경험

[개발 직무]
- 오픈소스 컨트리뷰션 경험
- 기술 블로그 운영 또는 컨퍼런스 발표 경험
- AWS/GCP 자격증 보유자

[데이터 직무]
- 석사 이상 학위
- Kaggle 또는 데이터 분석 대회 수상 경력
- 논문 게재 경험
```

---

### 2.4 복리후생 (benefits)

**정의:** 회사에서 제공하는 복지, 혜택, 근무 환경

**추출 원칙:**
- **있으면 원문 그대로** 추출 (요약 금지)
- **없으면 null** (빈 문자열 X)
- 회사마다 형식이 매우 다양함

**추출 대상 섹션 (참고용, 하드코딩 X):**
- "복리후생", "혜택", "Benefits", "처우"
- "우리는 이런 것들을 제공해요", "[ 복지 ]" 등

**예시:**
```
- 4대보험 및 퇴직금
- 자기계발비 연 100만원 지원
- 점심 식대 지원
- 유연근무제 (자율출퇴근)
- 리프레시 휴가 연 5일
- 최신 장비 지급 (맥북 프로, 모니터 등)
```

---

## 3. 추출 로직 설계

### 3.1 GPT 프롬프트 설계 원칙

```
핵심 원칙:
1. 섹션 헤더를 통해 해당 내용의 성격을 파악
2. 원문을 그대로 보존 (요약/축약/재구성 금지)
3. 해당 섹션이 명확히 존재할 때만 추출
4. 불확실하면 null 반환 (억지로 추출하지 않음)
5. bullet point, 번호, 들여쓰기 등 형식 유지
```

### 3.2 프롬프트 (추출 API용)

```
당신은 채용 공고(JD) 분석 전문가입니다.
JD에서 다음 정보를 추출하세요.

반환 형식:
{
  // 기존 필드
  "title": "포지션명",
  "clientCompany": "회사명",
  ...

  // 신규 필드
  "responsibilities": "주요업무/담당업무 섹션의 원문 (없으면 null)",
  "qualifications": "자격요건/필수요건 섹션의 원문 (없으면 null)",
  "preferredQualifications": "우대사항/우대요건 섹션의 원문 (없으면 null)",
  "benefits": "복리후생/혜택 섹션의 원문 (없으면 null)"
}

신규 필드 추출 규칙:

[responsibilities - 주요업무]
- "주요업무", "담당업무", "수행업무", "업무내용", "Role", "Responsibilities" 등의
  섹션 헤더 아래 내용을 찾아 원문 그대로 추출
- bullet point, 번호 등 원본 형식 유지
- 요약하거나 재구성하지 않음
- 해당 섹션이 명확히 없으면 null

[qualifications - 자격요건]
- "자격요건", "필수요건", "지원자격", "필수 사항", "Requirements", "Qualifications" 등의
  섹션 헤더 아래 내용을 찾아 원문 그대로 추출
- 경력, 학력, 스킬, 언어, 자격증 등 다양한 조건이 포함될 수 있음
- 조건의 맥락과 뉘앙스를 그대로 보존 (예: "5년 이상 보유하신 분" → 그대로 유지)
- 요약하거나 재구성하지 않음
- 해당 섹션이 명확히 없으면 null

[preferredQualifications - 우대사항]
- "우대사항", "우대요건", "우대 조건", "Preferred", "Nice to have", "Plus" 등의
  섹션 헤더 아래 내용을 찾아 원문 그대로 추출
- 요약하거나 재구성하지 않음
- 해당 섹션이 명확히 없으면 null

[benefits - 복리후생]
- "복리후생", "혜택", "Benefits", "처우", "복지" 등의
  섹션 헤더 아래 내용을 찾아 원문 그대로 추출
- 요약하거나 재구성하지 않음
- 해당 섹션이 명확히 없으면 null (빈 문자열이 아닌 null)

중요:
- 각 필드는 원문을 그대로 추출하며, AI가 임의로 요약/축약/재구성하지 않습니다.
- 섹션이 명확히 구분되지 않거나 해당 내용이 없으면 null을 반환합니다.
- JD마다 섹션 헤더 표현이 다를 수 있으므로 의미를 파악하여 유연하게 대응합니다.
```

---

## 4. 데이터베이스 스키마

### 4.1 Migration SQL

```sql
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- Migration: JD 상세 필드 추가
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

-- 주요업무 원문
ALTER TABLE positions
ADD COLUMN IF NOT EXISTS responsibilities TEXT;

-- 자격요건 원문
ALTER TABLE positions
ADD COLUMN IF NOT EXISTS qualifications TEXT;

-- 우대사항 원문
ALTER TABLE positions
ADD COLUMN IF NOT EXISTS preferred_qualifications TEXT;

-- 복리후생 원문
ALTER TABLE positions
ADD COLUMN IF NOT EXISTS benefits TEXT;

-- 코멘트
COMMENT ON COLUMN positions.responsibilities IS '주요업무/담당업무 원문 (JD에서 추출)';
COMMENT ON COLUMN positions.qualifications IS '자격요건/필수요건 원문 (JD에서 추출)';
COMMENT ON COLUMN positions.preferred_qualifications IS '우대사항/우대요건 원문 (JD에서 추출)';
COMMENT ON COLUMN positions.benefits IS '복리후생/혜택 원문 (JD에서 추출, 없으면 NULL)';
```

---

## 5. UI 설계

### 5.1 positions/new 페이지 카드 섹션 구성

```
┌─────────────────────────────────────────────────────────────────┐
│  새 포지션 등록                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📋 기본 정보                                             │   │
│  │ - 포지션명*, 회사명, 부서                                 │   │
│  │ - 직무 설명 (요약)                                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📝 주요 업무                                    [NEW]    │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ (textarea)                                          │ │   │
│  │ │ JD에서 추출된 주요업무 원문이 표시됩니다.            │ │   │
│  │ │ 직접 수정하거나 입력할 수 있습니다.                  │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ✅ 자격 요건                                    [NEW]    │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ (textarea)                                          │ │   │
│  │ │ JD에서 추출된 자격요건 원문이 표시됩니다.            │ │   │
│  │ │ 경력, 학력, 필수 스킬, 언어 등 모든 필수 조건 포함   │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ⭐ 우대 사항                                    [NEW]    │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ (textarea)                                          │ │   │
│  │ │ JD에서 추출된 우대사항 원문이 표시됩니다.            │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🎯 필수 스킬                                             │   │
│  │ - 추출된 스킬 텍스트 (textarea)                          │   │
│  │ - 스킬 태그 입력 + 태그 목록                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 💎 우대 스킬                                             │   │
│  │ - 추출된 스킬 텍스트 (textarea)                          │   │
│  │ - 스킬 태그 입력 + 태그 목록                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📚 경력 & 학력                                           │   │
│  │ - 경력 범위, 학력 요건, 우대 전공                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🎁 복리후생                                     [NEW]    │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ (textarea)                                          │ │   │
│  │ │ JD에서 추출된 복리후생 원문이 표시됩니다.            │ │   │
│  │ │ (없으면 이 섹션은 비어있음)                          │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🏢 근무 조건                                             │   │
│  │ - 근무지, 고용 형태, 연봉 범위, 마감일                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│                              [포지션 등록]                       │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 각 카드 섹션 컴포넌트 스펙

| 섹션 | 아이콘 | 필드 타입 | placeholder |
|------|--------|-----------|-------------|
| 주요 업무 | ClipboardList (blue) | textarea (rows=6) | "JD에서 추출된 주요업무가 표시됩니다. 직접 입력하거나 수정할 수 있습니다." |
| 자격 요건 | CheckCircle (green) | textarea (rows=6) | "JD에서 추출된 자격요건이 표시됩니다. 경력, 학력, 필수 조건 등이 포함됩니다." |
| 우대 사항 | Star (amber) | textarea (rows=5) | "JD에서 추출된 우대사항이 표시됩니다." |
| 복리후생 | Gift (purple) | textarea (rows=4) | "JD에서 추출된 복리후생이 표시됩니다. (선택사항)" |

---

## 6. 구현 상세

### 6.1 TypeScript 타입 확장

**파일: `types/position.ts`**

```typescript
export interface Position {
  // 기존 필드
  id: string;
  title: string;
  clientCompany?: string;
  department?: string;
  description?: string;
  requiredSkills: string[];
  preferredSkills: string[];
  minExpYears: number;
  maxExpYears?: number;
  requiredEducationLevel?: string;
  preferredMajors: string[];
  locationCity?: string;
  jobType: JobType;
  salaryMin?: number;
  salaryMax?: number;
  status: PositionStatus;
  priority: PositionPriority;
  deadline?: string;

  // 신규 필드 (JD 원문)
  responsibilities?: string;         // 주요업무 원문
  qualifications?: string;           // 자격요건 원문
  preferredQualifications?: string;  // 우대사항 원문
  benefits?: string;                 // 복리후생 원문
}
```

### 6.2 추출 API 인터페이스 확장

**파일: `app/api/positions/extract/route.ts`**

```typescript
interface ExtractedPosition {
  // 기존 필드
  title: string;
  clientCompany: string;
  department: string;
  description: string;
  requiredSkills: string[];
  preferredSkills: string[];
  minExpYears: number;
  maxExpYears: number | null;
  requiredEducationLevel: string;
  preferredMajors: string[];
  locationCity: string;
  jobType: string;
  salaryMin: number | null;
  salaryMax: number | null;
  deadline: string | null;

  // 신규 필드
  responsibilities: string | null;
  qualifications: string | null;
  preferredQualifications: string | null;
  benefits: string | null;
}
```

### 6.3 프론트엔드 State 추가

**파일: `app/(dashboard)/positions/new/page.tsx`**

```typescript
// 신규 State
const [responsibilities, setResponsibilities] = useState("");
const [qualifications, setQualifications] = useState("");
const [preferredQualifications, setPreferredQualifications] = useState("");
const [benefits, setBenefits] = useState("");

// JD 추출 결과 매핑
const handleExtraction = (extracted: ExtractedPosition) => {
  // 기존 필드 매핑...

  // 신규 필드 매핑
  setResponsibilities(extracted.responsibilities || "");
  setQualifications(extracted.qualifications || "");
  setPreferredQualifications(extracted.preferredQualifications || "");
  setBenefits(extracted.benefits || "");
};

// 폼 제출 시 데이터 구성
const submitData = {
  // 기존 필드...
  responsibilities: responsibilities || null,
  qualifications: qualifications || null,
  preferredQualifications: preferredQualifications || null,
  benefits: benefits || null,
};
```

### 6.4 임베딩 텍스트 개선

시맨틱 매칭 품질 향상을 위해 신규 필드를 임베딩에 포함:

```typescript
const embeddingText = [
  title,
  responsibilities,        // 주요업무 (신규)
  qualifications,          // 자격요건 (신규)
  preferredQualifications, // 우대사항 (신규)
  `필수 스킬: ${requiredSkills.join(", ")}`,
  `우대 스킬: ${preferredSkills.join(", ")}`,
  description,
].filter(Boolean).join("\n");
```

---

## 7. 구현 체크리스트

### Phase 1: 백엔드 (DB + API)
- [ ] DB 마이그레이션 파일 생성 (4개 컬럼 추가)
- [ ] `types/position.ts` 타입 확장
- [ ] `types/index.ts` 내보내기 업데이트
- [ ] `app/api/positions/extract/route.ts` - 프롬프트 및 인터페이스 수정
- [ ] `app/api/positions/route.ts` (POST) - 신규 필드 처리
- [ ] `app/api/positions/[id]/route.ts` (PATCH) - 신규 필드 처리

### Phase 2: 프론트엔드
- [ ] `app/(dashboard)/positions/new/page.tsx` - State 추가
- [ ] `app/(dashboard)/positions/new/page.tsx` - 카드 섹션 UI 추가
- [ ] `app/(dashboard)/positions/new/page.tsx` - 추출 결과 매핑
- [ ] `app/(dashboard)/positions/new/page.tsx` - 폼 제출 데이터 포함
- [ ] `app/(dashboard)/positions/[id]/page.tsx` - 상세 페이지 표시
- [ ] `app/(dashboard)/positions/[id]/edit/page.tsx` - 수정 페이지 표시

### Phase 3: 임베딩 및 최적화
- [ ] 임베딩 생성 로직에 신규 필드 포함
- [ ] 테스트 및 검증

---

## 8. 예상 결과

### Before (현재)
```
포지션: AI 기술 컨설턴트
설명: AI 기술 컨설팅 담당 (요약됨)
필수 스킬: [Python, AI, ML]
우대 스킬: [클라우드]
경력: 3년 이상
```

### After (개선 후)
```
포지션: AI 기술 컨설턴트
설명: AI 기술 컨설팅 담당

주요업무:
- 고객사 AI 사업 사전 컨설팅
- AI 사업 기술 검토 및 PoC 지원
- 핵심 기술 성능지표 관리
- AI 기술 트렌드 센싱 및 분석

자격요건:
- 학사(4년제) 이상
- 관련직무 경력 3년이상
- AI/ML/DL 기반 데이터 분석, 모델 개발 경험
- 생성형AI 모델 구조 및 모델 학습 관련 사전 지식/경험

우대사항:
- 석사 이상
- AI 관련 전공
- 관련 직무 5년 이상
- 클라우드/AI 관련 자격증 보유

필수 스킬: [Python, AI, ML, ...]
우대 스킬: [클라우드, ...]

복리후생:
- 4대보험, 퇴직금
- 자기계발비 지원
- 유연근무제
```

---

## 9. 결론

이 기획안은:
1. **하드코딩 없이** 다양한 JD 포맷에 유연하게 대응
2. **원문 보존**을 통해 정보 손실 최소화
3. **시맨틱 매칭** 품질 향상을 위한 풍부한 컨텍스트 확보
4. **사용자 편의성** 증대 (추출 후 최소 수정)

구현을 진행할까요?
