# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "libreoffice", "poppler_utils", "tesseract"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PYTHONUNBUFFERED = "1"
